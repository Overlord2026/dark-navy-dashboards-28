import React from 'react';
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Button } from '@/components/ui/button';
import { Users, UserCheck, Shield, Award } from 'lucide-react';
import { motion } from 'framer-motion';

interface FamilyOfficeTeamSlideProps {
  presentationMode?: boolean;
}

export const FamilyOfficeTeamSlide: React.FC<FamilyOfficeTeamSlideProps> = ({
  presentationMode
}) => {
  const familyOfficeTeamSlide = () => null;
  const familyOfficeTeamSlideDemo = true;
  
  return <div>Placeholder for Family Office Team Slide</div>;
};