import React from 'react';
import { ComingSoonPage } from '@/pages/ComingSoonPage';

export default function AdminSettingsPage() {
  return (
    <ComingSoonPage
      featureName="Platform Settings"
      description="System configuration, platform settings, and administrative controls for system administrators."
      expectedDate="Q1 2024"
    />
  );
}