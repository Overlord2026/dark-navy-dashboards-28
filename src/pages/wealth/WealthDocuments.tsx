import React from "react";
import { Navigate } from "react-router-dom";
import Documents from "@/pages/Documents";

const WealthDocuments = () => {
  // Reuse existing Documents component
  return <Documents />;
};

export default WealthDocuments;