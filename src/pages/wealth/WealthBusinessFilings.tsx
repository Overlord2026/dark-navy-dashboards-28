import React from "react";
import { Navigate } from "react-router-dom";
import BusinessFilings from "@/pages/BusinessFilings";

const WealthBusinessFilings = () => {
  // Reuse existing BusinessFilings component
  return <BusinessFilings />;
};

export default WealthBusinessFilings;