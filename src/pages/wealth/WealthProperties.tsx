import React from "react";
import { Navigate } from "react-router-dom";
import Properties from "@/pages/Properties";

const WealthProperties = () => {
  // Reuse existing Properties component
  return <Properties />;
};

export default WealthProperties;